# Core
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.10
aiofiles>=23.2.1

# Database
sqlalchemy>=2.0.0
greenlet>=3.0.0          # required by SQLAlchemy async; not auto-installed on py3.9/arm64
alembic>=1.13.0
asyncpg>=0.29.0          # async driver (live app)
psycopg2-binary>=2.9.0   # sync driver (Alembic migrations)

# Config
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Auth
python-jose[cryptography]>=3.3.0
httpx>=0.27.0

# LLM
google-generativeai>=0.8.0
anthropic>=0.34.0
openai>=1.0.0
groq>=0.11.0

# Ingestion
feedparser>=6.0.11
yt-dlp>=2024.1.0
beautifulsoup4>=4.12.0
requests>=2.32.0

# Storage (R2 / S3-compatible)
boto3>=1.34.0

# Cache / Queue
redis>=5.0.0

# Email (PurelyMail via SMTP)
aiosmtplib>=3.0.0

# Monitoring
sentry-sdk[fastapi]>=2.0.0

# Payments
stripe>=10.0.0
